changed_files=$(git diff --name-only HEAD HEAD~ -- '*.ts' '*.js')

if [ -n "$changed_files" ]; then
  echo "Running tests because TS/JS files changed:"
  echo "$changed_files"
  pnpm test
else
  echo "No TS/JS files changed. Skipping tests."
fi
